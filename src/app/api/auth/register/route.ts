 import { NextRequest, NextResponse } from "next/server";
 // req, res

 import { connectToDatabase } from "@/lib/db";
 import User from "@/models/user.model";

 export async function POST(req:NextRequest){ // datatype of req is NextRequest
    try {
        
    } catch (error) {
        
    }
 }